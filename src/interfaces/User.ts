// src/interfaces/User.ts
export interface User {
  id: string;
  name: string;
  email: string;
  // You can add more user-related fields here later, e.g.,
  // goals: string[];
  // streakDays: number;
}